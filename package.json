{
  "name": "bills-memory-keeper",
  "version": "1.0.0",
  "description": "A chat application to discuss Buffalo Bills history with an AI fan",
  "scripts": {
    "dev": "cd client && react-scripts start",
    "start": "cd client && react-scripts start",
    "dev:frontend": "cd client && react-scripts start",
    "build": "npm run build:frontend",
    "build:frontend": "cd client && react-scripts build",
    "vercel-build": "npm run build:frontend",
    "test": "cd client && react-scripts test",
    "clean": "rimraf dist client/build"
  },
  
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {
    "@types/node": "^16.18.59",
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "axios": "^1.11.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "typescript": "^4.9.5",
    "web-vitals": "^2.1.4"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.1",
    "rimraf": "^5.0.5"
  }
}
